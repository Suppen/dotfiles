import { type Config } from "prettier";

const config: Config = {
	printWidth: 120,
	tabWidth: 4,
	useTabs: true,
	trailingComma: "none",
	bracketSpacing: true,
	arrowParens: "avoid",
	endOfLine: "lf",
	quoteProps: "consistent"
}

export default config;
